{
  "name": "linguaflow",
  "version": "0.1.1",
  "private": true,
  "description": "AI-powered bilingual reading and language learning assistant (Chrome Extension, Manifest V3)",
  "type": "module",
  "scripts": {
    "build": "npm run build:main && npm run build:content",
    "build:main": "vite build --config vite.config.ts",
    "build:content": "vite build --config vite.content.config.ts",
    "dev": "npm run build && (vite build --watch --emptyOutDir false --config vite.config.ts & vite build --watch --emptyOutDir false --config vite.content.config.ts & wait)",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@tailwindcss/vite": "^4.1.0",
    "@types/chrome": "^0.0.326",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.5.0",
    "fake-indexeddb": "^6.0.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vitest": "^3.2.0"
  }
}
